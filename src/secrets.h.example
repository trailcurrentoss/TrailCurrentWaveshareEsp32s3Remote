#ifndef SECRETS_H
#define SECRETS_H

// ESP-NOW broadcast address of the gateway device
// Replace with your gateway's MAC address
uint8_t broadcastAddress[] = {0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF};

#endif
